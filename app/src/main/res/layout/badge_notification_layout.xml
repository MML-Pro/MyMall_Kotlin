<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingStart="8dp"
    android:focusable="true"
    android:clipToPadding="false"
    android:background="?attr/selectableItemBackground"
    android:paddingEnd="8dp">

    <ImageView
        android:id="@+id/notification_badge_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/baseline_notifications"
         />

    <TextView
        android:id="@+id/notification_badge_count"
        android:layout_width="@dimen/_12sdp"
        android:layout_height="@dimen/_12sdp"
        android:layout_marginStart="12dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/badge_count"
        android:gravity="center"
        android:text="0"
        android:textColor="@color/white"
        android:textSize="@dimen/_8ssp"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="@+id/notification_badge_icon"
        app:layout_constraintStart_toStartOf="@+id/notification_badge_icon">

    </TextView>

</androidx.constraintlayout.widget.ConstraintLayout>